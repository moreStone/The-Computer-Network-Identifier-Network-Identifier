<view class="comment-input">
  <input
    class="comment-field"
    value="{{inputValue}}"
    placeholder="写下你的评论..."
    bind:input="onInput"
    bind:confirm="onSubmit"
  />
  <button class="comment-btn" size="mini" bind:tap="onSubmit">发送</button>
</view>
