<view class="search-bar">
  <view class="search-icon">🔍</view>
  <input
    class="search-input"
    value="{{value}}"
    placeholder="{{placeholder}}"
    focus="{{autofocus}}"
    bind:input="onInput"
    bind:confirm="onConfirm"
  />
</view>
